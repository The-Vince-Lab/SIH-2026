# SkillTrace AI backend - production dependencies (trimmed for Render / clean PyPI install).
# NOTE: emergentintegrations & the Emergent-internal litellm wheel were removed because
# they are NOT used by this app and break `pip install` on Render.

# --- Web framework ---
fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2
python-multipart==0.0.32

# --- Validation / config ---
pydantic==2.13.5
email-validator==2.3.0
python-dotenv==1.2.3

# --- MongoDB (dnspython is required for mongodb+srv:// Atlas URIs) ---
motor==3.3.1
pymongo==4.6.3
dnspython==2.8.0

# --- Auth & crypto ---
PyJWT==2.13.0
bcrypt==4.1.3
cryptography==50.0.1

# --- ML / analytics ---
scikit-learn==1.9.0
numpy==2.4.6
scipy==1.17.1
pandas==3.0.5
joblib==1.6.0
RapidFuzz==3.14.6

# --- PDF / report export ---
reportlab==5.0.1
